export type Packages = {
  id: string;
  is_recommanded: boolean;
  link: string;
  created_at: string;
  title: string;
  image: string;
  description: string;
  row: string;
  is_active: boolean;
};
